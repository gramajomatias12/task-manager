import { Routes } from '@angular/router';
import { TareasList as TareasListComponent } from './components/tareas-list/tareas-list';

export const routes: Routes = [
  { path: '', component: TareasListComponent }
];

